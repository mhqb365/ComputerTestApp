<UserControl x:Class="ComputerTestApp.Views.BatteryTestControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Unloaded="UserControl_Unloaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{DynamicResource BatteryTitle}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{DynamicResource BatteryDescription}" Margin="0,0,0,20"
                       Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"/>
        </StackPanel>

        <Button Grid.Row="1" x:Name="CheckBatteryButton" Content="{DynamicResource CheckBattery}"
                Width="200" Height="50" FontSize="16" HorizontalAlignment="Left"
                Click="CheckBatteryButton_Click" Style="{StaticResource ButtonPrimary}"/>

        <ScrollViewer Grid.Row="2" Margin="0,24,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ResultPanel"/>
        </ScrollViewer>
    </Grid>
</UserControl>
